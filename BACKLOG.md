# Бэклог — баги и улучшения

Живой список для будущих исправлений. Помечаю приоритет: 🔴 высокий, 🟡 средний, 🟢 низкий/полировка.
Статус: `[ ]` открыт, `[x]` сделано.

---

## Критичное — основной сценарий сломан

- [x] 🔴 **Сохранение тренировки зависает («бесконечно сохраняет»).** ~~После выбора упражнения и ввода подходов кнопка сохранения крутится бесконечно.~~ Исправлено 23.06: добавлен таймаут запросов (`src/lib/withTimeout.js`, 15 c) — теперь при «зависшей» сети промис отклоняется с понятной ошибкой, спиннер не крутится вечно; вставки `workout_exercises`/`sets` объединены в батч (меньше round-trip'ов на телефоне).
- [x] 🔴 **Сохранённые тренировки нигде не отображаются.** Исправлено 23.06: добавлен экран **«История»** (новая вкладка) — список тренировок по датам с содержимым (упражнения + подходы кг×повт.). `src/screens/HistoryScreen.jsx`.
- [x] 🔴 **История должна быть редактируемой и наглядной.** Исправлено 23.06: запись можно открыть → править (вес/повторы, +/− подход, убрать/добавить упражнение через пикер) и удалить. Каждая карточка показывает дату + сводку (N упр · M подх.).

## Мобильная вёрстка (приоритет — общий вид на телефоне «не очень приятный»)

- [x] 🔴 **Пикер упражнений пустой до начала поиска.** Проверено 23.06: в текущем коде пикер по умолчанию показывает весь справочник (пустой запрос → все упражнения), поиск лишь сужает. Баг был из старой версии; поведение корректно.
- [ ] 🟡 **Возможность вводить свои упражнения.** В справочнике может не быть нужного — дать кнопку «добавить своё упражнение» прямо из пикера (с выбором группы), сохранять в общий справочник. (Было в ТЗ; подтверждено отзывом, 23.06.) _Не входило в багфикс-проход 23.06._
- [x] 🔴 **Фильтр-чипы категорий накладываются на список.** Исправлено 23.06 (`src/index.css`): шапка/поиск/чипы получили `flex-shrink: 0` (список их больше не сжимает), список — `flex: 1; min-height: 0` с отделяющей рамкой; высота `.sheet` фиксирована.
- [x] 🔴 **Активный чип (зелёный) перекрывает подпись.** Исправлено 23.06: чипу заданы `display: inline-flex; align-items: center; line-height: 1.2; min-height: 36px` — подпись больше не подрезается.
- [x] 🟡 **Строка чипов горизонтально скроллится** — `overflow-x: auto`, `flex-shrink: 0` на чипах + `-webkit-overflow-scrolling: touch`.
- [ ] 🟡 **Общая полировка отступов на мобиле.** Десктоп выглядит ок, мобайл — тесно. Пройтись по паддингам, размерам шрифтов, высоте кликабельных зон (минимум ~44px под палец).
- [ ] 🟢 **Модалка пикера на весь экран на мобиле** — заголовок «Упражнение» + «закрыть» и поиск зафиксировать сверху (sticky), список скроллить под ними.

## Функциональность (из следующих проходов ТЗ — held)

> ⚠️ Часть пунктов ниже в ТЗ числятся как **MVP**, а не фаза 2 (помечено `[MVP по ТЗ]`). Решение отложить нужно зафиксировать явно, иначе всплывёт на приёмке.

- [ ] 🔴 **Офлайн-режим + синхронизация** (IndexedDB, очередь записей). `[MVP по ТЗ: п. 4.1.5, разделы 6–7, критерий готового MVP в Прил. C]` Сейчас есть только PWA-оболочка (`vite-plugin-pwa`) — локального хранилища и синка нет.
- [ ] 🔴 **Общая лента тренировок друзей.** `[MVP по ТЗ: раздел 7]` В коде отсутствует. (Раньше была слита с лидербордом — разделил.)
- [ ] 🟡 **Лидерборд по жиму (1ПМ).** ⚠️ Противоречие в самом ТЗ: раздел 7 → фаза 2, но раздел 8 и критерий готового MVP (Прил. C) → MVP. Нужно согласовать статус.
- [ ] 🟡 Шаблоны тренировок. (Фаза 2 по ТЗ.)
- [ ] 🟢 Админка: управление справочником и пользователями, сброс/смена PIN из интерфейса. `[в ТЗ роль admin входит в MVP-набор раздела 7]`
- [ ] 🟢 График веса тела. (Фаза 2, согласованное решение №4.)
- [ ] 🟢 Теги дня (грудь+бицепс и т.п.) и фильтр по ним. (Фаза 2 по ТЗ.)

## Пропущенные требования ТЗ (выпали — не отслеживались)

> Эти пункты есть в ТЗ, но их не было ни в бэклоге, ни в коде. Свериться при разработке.

- [x] 🔴 **Авто-рекорды (PR).** Проверено 23.06: `ProgressScreen` уже помечает новый максимум 1ПМ по ходу истории (`isPr`) — рекордные точки на графике жёлтые + легенда. Базовое выделение PR есть. (Бейджи-уведомления — отдельный 🟢-пункт ниже.)
- [ ] 🟡 **Анти-дубли при добавлении упражнения.** ТЗ 3.2/4.4: при добавлении своего упражнения показывать похожие по названию, чтобы не плодить дубли.
- [ ] 🟡 **Быстрый повтор предыдущего подхода в один тап** + крупные кнопки +/−. ТЗ 4.1.4 — ключевой элемент быстрого ввода в зале.
- [ ] 🟢 **Внутренние бейджи-уведомления.** ТЗ 4.5 (MVP-вариант): «у тебя новый рекорд», «друг побил твой рекорд».
- [ ] 🟢 **Подсветка прогресса/регресса** на графике (рост — зелёным, спад — красным). ТЗ 4.2 — проверить, реализовано ли в `ProgressScreen`.

## Безопасность / техдолг

- [ ] 🟡 **Смена пароля (PIN) из приложения.** Пользователь может сам поменять свой PIN в настройках/профиле (ввести текущий → новый → подтверждение). Сейчас PIN задаётся только в `seed.sql`. (Запрос, 23.06.)
- [ ] 🟡 PIN хэшируется SHA-256 без соли — для MVP ок, позже усилить (соль / Supabase Auth).
- [ ] 🟢 Размер JS-бандла >500 КБ (vite предупреждает) — code-splitting / manualChunks при росте проекта.

---

_Заметка: добавляй сюда новые находки — буду разбирать по приоритету в отдельных проходах._

---

### Лог проходов

**Багфикс-проход 23.06.2026.** Закрыты все 🔴-баги основного сценария и мобильной вёрстки: зависание сохранения (таймаут + батч), экран «История» с просмотром/правкой/удалением, наезд чипов в пикере, проверены пикер-по-умолчанию и выделение PR. **Сознательно отложены** два 🔴 из раздела «held» — это крупные новые фичи, а не баги: офлайн-режим + синхронизация (IndexedDB/очередь) и общая лента тренировок друзей. Их стоит брать отдельным фиче-проходом. Изменённые файлы: `WorkoutScreen.jsx`, `App.jsx`, `index.css`, новые `HistoryScreen.jsx` и `lib/withTimeout.js`.
