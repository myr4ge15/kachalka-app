node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
.vscode/*
!.vscode/extensions.json
# локальные разрешения Claude Code (специфичны для машины); общий .claude/ можно коммитить
.claude/settings.local.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# артефакты Playwright (e2e): отчёты, трассы, видео
test-results/
playwright-report/
blob-report/
.playwright/

# Рабочая документация — держим только локально (см. AGENTS.md, «Где задачи и документация»)
ТЗ.md
BACKLOG.md
WISHLIST.md
CHANGELOG-друзьям.md
concept.html
archive/legacy-claude/
ПАСПОРТ.md
PLAN-*.md
setup-supabase-github.md
# подпапки docs/ (планы уже ловятся PLAN-*.md; ревью и гайды — по папкам)
docs/reviews/
docs/guides/
# кликабельные прототипы/мокапы — целиком
prototypes/
# локальный мусор в корне (старые скриншоты, вендорные бинарники)
debug/
deno/

# Настройка БД (схема, сид, RPC) и рабочие доки живут в ОТДЕЛЬНОМ ПРИВАТНОМ репозитории:
# этот публичный, а выкладывать RLS-политики, тела админских RPC и внутренние заметки
# живого приложения незачем. Инструкция — docs/guides/приватный-репо.md.
supabase/

# git-каталог того самого приватного репозитория (рабочая папка у них общая)
.private-git/

# разовый локальный генератор PNG-иконок (рецепт пересборки — в docs/ПАСПОРТ.md), в репо не нужен
scripts/gen-icons.mjs
